"""Framework-free AML engines.

These modules contain the compliance intellectual property and intentionally
depend only on the Python standard library (plus ``rapidfuzz`` for screening),
so they can be unit-tested in isolation, embedded in batch jobs, or invoked
from the API layer interchangeably.
"""
