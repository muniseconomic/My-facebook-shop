"""Service / orchestration layer.

Bridges the framework-free core engines with the persistence layer and exposes
coarse-grained operations (onboard a customer, screen a name, run monitoring,
manage cases, produce reports) that the API and batch jobs call.
"""
